import Signup from "@/pages/Signup";
import { Metadata } from "next";

export const metadata: Metadata = {
    title: 'Friends signup',
    description: 'Signup with your email and password'
}

export default Signup